// Root build file for the 2x2 (2X2) wallet.
// Plugin versions are declared here and applied in the module build files.
plugins {
    id 'com.android.application' version '8.5.2' apply false
    id 'com.android.library' version '8.5.2' apply false
}
